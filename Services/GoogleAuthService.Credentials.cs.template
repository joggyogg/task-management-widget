// Local OAuth credentials. This file is GITIGNORED — paste your real values here.
// For installed-app desktop OAuth, the "secret" is not actually confidential per Google's
// documentation; it ships in the built binary. We still keep it out of source control so the
// repo doesn't show up in secret-scanning indexes used by abuse bots.

namespace TaskManagementWidget.Services
{
    public static partial class GoogleAuthService
    {
        public const string ClientId     = "PASTE_YOUR_CLIENT_ID.apps.googleusercontent.com";
        public const string ClientSecret = "PASTE_YOUR_CLIENT_SECRET";
    }
}
